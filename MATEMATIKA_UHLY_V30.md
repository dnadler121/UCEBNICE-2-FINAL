# V30 – úhly ve stupních
- `30deg`, `117deg` atd. se studentovi zobrazují jako `30°`, `117°`.
- `deg` není studentský input; celý číselný úhel je pevná konstrukce.
- Převod funguje i uvnitř `sin(...)`, `cos(...)`, `tan(...)`.
- `alpha` se ve studentském zobrazení převádí na `α`.
