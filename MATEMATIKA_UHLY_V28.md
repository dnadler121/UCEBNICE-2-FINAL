# Matematika – zápis úhlů V28

Učitel může zapisovat úhly pomocí `deg`:
- `30deg` → 30°
- `45deg` → 45°
- `60deg` → 60°
- `sin(30deg)` → sin 30°
- `cos(60deg)` → cos 60°
- `alpha` → α
- `sin(alpha)` → sin α

Ve studentské verzi jsou `sin`, `cos`, `alpha` a úhly zapsané pomocí `deg` pevné konstrukce.
